<h1><%= t('views.common.title_new', name: Comment.model_name.human.downcase) %></h1>

<%= render "form", commentable: @commentable, comment: @comment %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Report.model_name.human.downcase)), polymorphic_path(@commentable.class) %>
</nav>
