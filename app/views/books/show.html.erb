<p style="color: green"><%= t(notice) %></p>

<div class="show-item">
  <%= render @book %>
</div>

<div>
  <nav class="page-nav">
    <%= link_to t('books.actions.edit'), edit_book_path(@book) %> |
    <%= link_to t('pages.books.actions.back'), books_path %>

    <%= button_to t('books.actions.destroy'), @book, method: :delete %>
  </nav>
</div>
