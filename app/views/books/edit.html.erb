<h1><%= t('pages.books.title.edit') %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <nav class="page-nav">
    <%= link_to t('books.actions.show'), @book %> |
    <%= link_to t('pages.books.actions.back'), books_path %>
  </nav>
</div>
